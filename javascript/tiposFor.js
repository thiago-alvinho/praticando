let frutas = ['morango', 'maça', 'pera', 'manga', 'abacaxi'];

//frutas.forEach( (fruta, indice) => {
//    console.log(`Indice ${indice}, fruta ${fruta}`);
//})

for(const fruta of frutas) {
    console.log(`fruta: ${fruta}`);
}